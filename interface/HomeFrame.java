public class HomeFrame extends BaseFrame {
    public HomeFrame(DataModel model) {
        super(model);
    }

    @Override
    public void initComponents() {
        super.initComponents();
        // Thêm các thành phần UI riêng của MainFrame vào đây
    }
}
